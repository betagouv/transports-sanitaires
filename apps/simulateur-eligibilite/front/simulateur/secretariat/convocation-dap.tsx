// Texte livré mot pour mot (contrat v9.7.1, `application.mjs:274`) : une DAP
// née d'une convocation (longue distance) en garde la trace auprès de
// l'Assurance Maladie.

/** Phrase ajoutée au Bloc 1 quand la DAP vient d'une convocation. */
export function ConvocationJointeALaDap() {
  return (
    <p>
      Conservez la convocation et joignez-la à la demande pour justifier le
      motif du déplacement.
    </p>
  );
}
